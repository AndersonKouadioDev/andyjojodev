name: Lighthouse CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  lighthouse:
    name: Lighthouse Audit
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build
        run: bun run build
        env:
          NODE_ENV: production

      - name: Start server
        run: bun run start &
        env:
          PORT: 3000

      - name: Wait for server
        run: npx wait-on http://localhost:3000/fr --timeout 60000

      - name: Warm up server
        run: |
          curl -sf http://localhost:3000/fr > /dev/null
          curl -sf http://localhost:3000/fr/projects > /dev/null
          curl -sf http://localhost:3000/fr/skills > /dev/null
          sleep 10

      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v12
        with:
          urls: |
            http://localhost:3000/fr
            http://localhost:3000/fr/projects
            http://localhost:3000/fr/skills
          configPath: .github/lighthouserc.json
          budgetPath: .github/lighthouse-budget.json
          uploadArtifacts: true
          temporaryPublicStorage: true
